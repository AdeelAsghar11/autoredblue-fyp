"""Agent nodes for the AutoRedBlue LangGraph pipeline.

See docs/ARCHITECTURE.md "Agent-by-agent" for the full pipeline
(scope check -> recon -> approval gate -> scan -> triage -> report ->
verify) and docs/ARCHITECTURE.md "Team ownership" for who owns which
agent.
"""
