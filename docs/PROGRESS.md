# PROGRESS.md: AutoRedBlue

> Current status. Update at the end of every work session. Keep it short: what's done, what's next, what's blocking.

## Phase
FYP-I, build underway. Proposal approved by supervisor (Dr Saeed Ur Rehman). Architecture finalised and research-verified; graph-engineering scope and the two-machine hardware plan are now explicit. Environment set up, DVWA running, first real code (scope allow-list check) landed.

## Done
- [x] Idea selected and scoped (AutoRedBlue: local multi-agent web-app security audit).
- [x] Proposal written, approved by supervisor.
- [x] Supervisor feedback incorporated: two-pass (unauthenticated + authenticated) scanning defined; target profile defined as Pakistani public-sector portals, with DVWA/Juice Shop/Metasploitable as legal stand-ins.
- [x] Competitive + academic research round completed (findings 1–4).
- [x] Architecture finalised: 4 agents → **5 agents** (added Verification Agent); tool-calling pattern set (plain-text-reason-then-parse); planning stance set (code owns sequencing).
- [x] Context files created (PROJECT / ARCHITECTURE / DECISIONS / PROGRESS / RESEARCH).
- [x] **All research claims independently verified (2026-08-25).** Almost all real; 4 corrections (Excalibur is actually PentestGPT V2; Red-MIRROR & PoC-Adapt are exploitation-side; Shannon is cloud/Claude-API-based, not local); 1 dropped (NRT-Bench, actually about nuclear-plant operator agents, irrelevant). No decisions changed. See RESEARCH.md verification log.

## Next concrete task
**No OS install needed to start.** "Platform: Linux" on the proposal is the deployment-target story, not a development blocker. Ollama, Docker, Nmap, OWASP ZAP, sqlmap, Playwright, and the whole Python stack (LangGraph/FastAPI/ChromaDB/PyWebView) all run natively on Windows. Only Nikto is genuinely annoying on native Windows, use WSL2 for it when you get there (`wsl --install`, no dual-boot needed), not before. Lab PC's actual OS still unconfirmed, check before it matters for the real evaluation run.

**The full, granular, dependency-ordered build plan now lives in `ROADMAP.md`.** Check the first unchecked box there and start. This section used to list the first few steps directly; that became a second copy of the same plan the moment `ROADMAP.md` existed, so it's gone from here on purpose, not by accident.

**2026-09-25 environment setup session (dev PC):**
- `uv venv` created at `.venv` (Python 3.11.15, matches the `>=3.11` pin), `uv pip install -e ".[dev]"` done, `uv.lock` generated. All ROADMAP-0 Python deps (LangGraph, FastAPI, ChromaDB, Ollama client, Playwright, PyWebView, PyYAML, pytest) import cleanly.
- `.env` and `config/allowlist.yaml` / `config/settings.yaml` created from their `.example` templates (still gitignored, as intended).
- Ollama service started and confirmed working: `qwen2.5:7b` and `qwen2.5-coder:7b` (the model `.env.example`/`ARCHITECTURE.md` actually specify) both pulled and tested, including a reason-then-tool-call-style prompt. `nvidia-smi` confirms the GTX 1660 Super loads the 7B model at ~4.7GB/6GB VRAM, in line with the ROADMAP's sizing assumption.
- Known non-blocking gap: Playwright's own `playwright install chromium` browser-binary download times out against `cdn.playwright.dev` from this network. Not needed until step 9.3 (FYP-II); retry later or via a different network.

**2026-09-25 follow-up (same day): Docker unblocked.** The Hyper-V/Virtual Machine Platform fix from earlier in the session (elevated `dism.exe /online /enable-feature`, reboot) took: `wsl -d docker-desktop` now starts cleanly instead of failing with `HCS_E_HYPERV_NOT_INSTALLED`. Started Docker Desktop, waited for the engine, ran `docker run hello-world`: it pulled and ran successfully. ROADMAP 0.1 and 0.2 both fully pass now (Ollama half already confirmed above, Docker half confirmed just now). Boxes checked, committed, pushed.

**2026-09-25, same session: ROADMAP 0.3 done.** `docker compose up -d dvwa` brings up `vulnerables/web-dvwa`, confirmed listening on `localhost:8080` with the real DVWA login page (`<title>Login :: Damn Vulnerable Web Application (DVWA) v1.10 *Development*</title>`). Box checked, committed, pushed. Next up: 0.4 (manually run Nmap and ZAP against DVWA by hand, save sample output, tool-literacy step) then module 1 (allow-list check).

**2026-09-26, ROADMAP 0.4 done.** Installed Nmap 7.80 via winget (clean, fast). OWASP ZAP's native Windows installer stalled at a fixed byte count downloading from GitHub releases, twice in a row (same class of CDN-download flakiness already seen with Playwright's browser binaries on this network); switched to the official `zaproxy/zap-stable` Docker image instead, which pulled fine (Docker Hub layers unaffected). Ran `nmap -sV -p 8080 --script=http-title,http-headers localhost`, confirmed DVWA (Apache 2.4.25, DVWA v1.10 login page), saved to `tests/fixtures/nmap_dvwa.{txt,xml}`. Ran a ZAP baseline (passive) scan via `docker run zaproxy/zap-stable zap-baseline.py -t http://host.docker.internal:8080`, 16 real WARN-level findings (missing security headers, cookie flags, debug error disclosure, etc.), saved HTML/JSON reports plus the automation-framework plan to `tests/fixtures/zap_dvwa_report.{html,json}` and `zap.yaml`. Box checked, committed, pushed. Note for step 6.1 (`zap_tool.py`): the Docker image plus `zap-baseline.py --auto` pattern used here worked cleanly and is a reasonable basis for the real tool wrapper, no need to fight the native Windows installer again. Next up: module 1 (allow-list check, `config/allowlist.yaml`).

**2026-09-26, ROADMAP 1.1 + 1.2 done (module 0 complete).** `config/allowlist.example.yaml`'s schema (`allowed_targets` list of `host`/`port`/`description`) was already in good shape from initial scaffolding, confirmed and checked off. Implemented `src/autoredblue/agents/scope_check.py::is_in_scope(target, allowlist_path=None)`: parses a `"host:port"` target string, loads `config/allowlist.yaml`, exact-matches host and port. Fails closed on any problem with the file's *contents* (empty file, invalid YAML, missing/wrong-typed `allowed_targets`, malformed entries all return an empty allow-list, so nothing matches); a missing allow-list *file* raises `FileNotFoundError` instead, since that is a setup mistake, not an empty scope. 8 unit tests in `tests/test_agents/test_scope_check.py` cover: allowed target passes, unlisted target refused, missing port refused, empty file, malformed YAML, missing `allowed_targets` key, wrong-typed `allowed_targets`, missing file raises. All pass; also smoke-tested against the real local `config/allowlist.yaml` (DVWA on `localhost:8080` allowed, arbitrary hosts refused). This is genuinely the first real pipeline code in the repo. Next up: module 2 (minimal graph skeleton, `src/autoredblue/graph/state.py` then the 2-node scope-check-to-stub-recon graph).

**2026-09-26, ROADMAP 2.1 + 2.2 done (module 2 complete).** `AuditState` (TypedDict) now carries the full state list from ARCHITECTURE.md plus one graph-internal field, `scope_allowed` (set by the scope-check node, used to route the graph; not in ARCHITECTURE.md's list since that predates the graph wiring). Only `target` is required to construct it, everything else is `NotRequired`. Confirmed it constructs and round-trips through `json.dumps`/`json.loads`. Wired the real 2-node graph in `src/autoredblue/graph/build_graph.py` with LangGraph's `StateGraph`: `scope_check` (calls the real `is_in_scope` from module 1) routes via a conditional edge to `recon` (a stub node that just logs "recon ran" and writes a placeholder `recon_findings`) or straight to `END`. 4 new tests confirm both cases: an allowed target fires both nodes in order and returns the stub recon result; a disallowed target stops after `scope_check`, no "recon ran" log line, no `recon_findings` key at all. Also manually smoke-tested end to end against the real DVWA allow-list. 12/12 tests pass across the whole repo. This proves the scope-gate mechanic actually works before any real tool gets wired into the recon node. Next up: module 3 (real Recon Agent, starting with wrapping Nmap as a callable tool, step 3.1).

**2026-09-26, ROADMAP 3.1 done.** `src/autoredblue/tools/nmap_tool.py`: `run_nmap(host, *args)` shells out to Nmap (falls back to the Windows install path if not on PATH) and forces `-oX -` so output is always parseable XML; `parse_nmap_xml()` turns that into a structured list of open ports (port, protocol, state, service, product, version), dropping closed/filtered noise; `scan_target()` chains both. 3 unit tests, including one that parses the real Nmap XML saved in step 0.4 and checks it matches exactly what was seen by hand (8080/tcp, Apache 2.4.25). Also live-smoke-tested against the still-running DVWA container: `scan_target('localhost', '-sV', '-p', '8080')` returns the identical real result. 15/15 tests pass repo-wide. Also: from this point on, commits in this repo carry no Claude co-authorship attribution (user preference); the 8 commits made earlier in this session were rewritten and force-pushed to drop it. Next up: 3.2 (wrap subfinder).

**2026-09-26, ROADMAP 3.2 done.** subfinder wasn't installed and isn't on winget; downloaded the official Windows binary release directly from GitHub (`curl`, not winget this time, worked cleanly at 3.2MB/s, no CDN-stall repeat) to `C:\Users\adeel\bin\subfinder.exe`, already on PATH. `src/autoredblue/tools/subfinder_tool.py`: `enumerate_subdomains(domain)` runs `subfinder -d <domain> -silent`, returns a clean list of lines. Confirmed live against a deliberately nonexistent domain: exits 0, empty stdout, wrapper returns `[]`, no exception, exactly the required behavior. (Aside: running it against literally "localhost" returns noisy junk, unrelated orgs' internal certs that happen to end in ".localhost", so real recon runs need a real domain, not "localhost", when this gets wired into the Recon Agent in 3.5.) 2 unit tests mock subprocess.run to cover empty-result and multi-result parsing without depending on the network or the binary being installed, so they run anywhere. 17/17 tests pass repo-wide. Next up: 3.3 (the plain-text-reason-then-parse LLM pattern, the project's #1 risk, first real test of it).

**2026-09-26, ROADMAP 3.3 done, first real result on risk #1.** Implemented the actual tool-calling pattern from ARCHITECTURE.md: `llm/parsing.py::parse_action()` extracts a `<action><tool>...</tool><args>{...json...}</args></action>` block from loose model output via regex, ignoring any reasoning text around it, raising `ParseError` (not a bare exception) on any malformed shape (missing tags, empty tool, invalid JSON, args not an object) so the future retry loop (3.4) can catch it specifically. `llm/ollama_client.py::generate()` is now a real Ollama client call (host/model resolved from env vars, then `config/settings.yaml`, then a hardcoded fallback). `agents/recon.py::recon_perceptor()` wires them together: prompts the model with raw Nmap text and a strict `<action>` format spec, parses the response, returns the structured `open_ports` findings.

Ran the actual required reliability check: `recon_perceptor()` called 10 times against the real Nmap text saved in step 0.4 (`qwen2.5-coder:7b` via the now-running Ollama server), each a fresh live model call. **Result: 10/10 successful** (well above the required 8/10), every single one correctly identified port 8080/tcp/http/Apache, with only minor, harmless variance in how "Apache httpd 2.4.25 (Debian)" got split across the `product`/`version` fields. This is the first real evidence the project's #1 flagged risk (small-model tool-calling reliability) actually holds up in practice, not just in theory. 14 new unit tests (parsing edge cases, Ollama client config resolution, recon_perceptor with a mocked LLM) all pass without needing a live model, so the automated suite stays fast and CI-safe; the 10-trial live check itself was a one-off manual verification run, not added to the pytest suite. 31/31 tests pass repo-wide. Next up: 3.4 (the retry loop on parse failure).

**2026-09-26, ROADMAP 3.4 done.** `graph/build_graph.py::build_reason_then_parse_graph(reason_fn, parse_fn, max_attempts=3)` implements ARCHITECTURE.md's retry pattern as a real LangGraph cycle, not a plain Python loop: a `reason` node calls `reason_fn(state)` for a completion, a `parse` node runs `parse_fn` (catching `ParseError`), and a conditional edge routes success to `END`, routes back to `reason` on failure while attempts remain (with the previous `llm_error` available in state for the next prompt to use), and routes to a graceful `END` with `llm_failed=True` once `max_attempts` is hit, rather than raising. New generic state fields (`llm_completion`, `llm_error`, `llm_attempts`, `llm_result`, `llm_failed`) support this and are reusable by any future reason-then-parse node, e.g. the Scan Agent's tool-selection loop later. Deliberately kept generic rather than recon-specific, since 3.5 will supply the real `recon_perceptor` as `reason_fn`. 3 new tests using a scripted fake `reason_fn` (fails twice then succeeds; always fails; succeeds immediately) all pass, confirming: retries happen and recover, repeated failure ends gracefully at exactly 3 attempts with no exception raised, and success on the first try doesn't retry at all. 34/34 tests pass repo-wide. Next up: 3.5 (wire 3.1-3.4 together as the real Recon Agent, replacing the graph's stub recon node from step 2.2).

**2026-09-26, ROADMAP 3.5 done, module 3 complete.** `agents/recon.py::run_recon(state)` is now real: parses `state["target"]` ("host:port"), runs Nmap in plain-text (-oN) format (added an `output_format` option to `nmap_tool.run_nmap`, defaulting to the existing XML behavior so step 3.1 stays unaffected), runs subfinder against the host, and turns the raw Nmap text into structured `open_ports` findings via the reason-then-parse retry loop from step 3.4, capped at 3 attempts; on repeated failure it records the error in `recon_findings` instead of crashing. Moved `build_reason_then_parse_graph` out of `graph/build_graph.py` into its own `graph/retry_loop.py` module to avoid a circular import (`agents/recon.py` needs it, and `graph/build_graph.py` needs `agents/recon.py`). `graph/build_graph.py`'s `recon` node is now the real `run_recon`, the stub from step 2.2 is gone.

Live end-to-end smoke test against the real running DVWA (real Nmap, real subfinder, real Ollama, no mocks): `build_graph().invoke({"target": "localhost:8080"})` correctly reports port 8080/tcp/http/Apache httpd 2.4.25, exactly matching what's already known to be there from step 0.4. subfinder against "localhost" again returns the same noisy unrelated-orgs'-certificate junk seen in step 3.2's testing, expected and harmless, just not a meaningful signal for this kind of target. Rewrote `test_build_graph.py`'s integration tests to mock the tools/LLM (deterministic, CI-safe) confirming the same scope-gate-then-recon wiring; added 2 new `run_recon`-specific tests covering the success path and the graceful-failure-after-3-attempts path. 36/36 tests pass repo-wide.

**Module 3 (Real Recon Agent) is now fully complete: 3.1 through 3.5, the project's core reliability pattern proven end to end for real, not just in theory.** Next up per ROADMAP: module 4 (Human Approval Gate).

## Open risks / watch-items
- **#1 risk, unchanged:** small-model tool-calling reliability. The plain-text-reason-then-parse decision (with its retry loop) is the mitigation; validate it in the thin slice before committing to it everywhere.
- **Graph/orchestration engineering is real, distinct scope**, not something LangGraph provides for free: retry loops, conditional routing, the approval-gate interrupt, and the Verification Agent's cross-session resume all need to be explicitly built. See ARCHITECTURE.md's new "Graph & orchestration engineering" section.
- **Dev machine (1660 Super, 6GB) is below the lab machine (A4000, 16GB VRAM)**: expect slower inference and a smaller model during daily development; don't judge reliability from the dev machine alone, validate on the lab PC before drawing conclusions.
- **Learning curve:** neither team member has security-tooling experience. Budget FYP-I time for TryHackMe / PortSwigger Web Security Academy alongside the build (bounded: OWASP Top 10 literacy, not becoming a researcher).
- **Parsing overhead:** Nmap (XML), ZAP (JSON), Nikto, sqlmap all output differently. Budget explicit time for normalisation before agent logic sits on top.
- **Citation hygiene (resolved but stay careful):** research claims are now verified, but when writing the report cite the *corrected* names/facts from RESEARCH.md (PentestGPT V2 not "Excalibur"; do not cite NRT-Bench; Shannon is cloud-based). Re-verify anything new before it goes in.

## Not started
- All five agents (design done, no code).
- ChromaDB / RAG knowledge base.
- Evaluation harness (precision/recall vs DVWA & Juice Shop ground truth; local-vs-cloud model comparison).
- Deployment packaging.
